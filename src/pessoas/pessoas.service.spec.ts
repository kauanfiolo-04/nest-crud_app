describe('Pessoas service', () => {
  beforeEach(() => {
    console.log('Isso roda antes de cada teste');
  });

  it('It Alguma coisa', () => {
    const numero1 = 1;
    const numero2 = 2;

    const result = numero1 + numero2;

    expect(result).toBe(3);
  });
});
